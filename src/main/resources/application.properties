## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/signup
spring.datasource.username = root
spring.datasource.password = 12345

spring.jpa.show-sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
#Using update for spring.jpa.hibernate.ddl-auto property is fine for development.
# But, For production, You should keep the value of this property to “validate”, and

spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=true
server.port=3000

#debug=true
#logging.file=C:/Users/tcokayildirim/IdeaProjects/signupproject/logs/signupproject.log